**/proto/generated
.env
.VSCodeCounter*
.vscode
logs*
ui/node_modules
next-env.d.ts
.next
**/*.pprof
**/*.prof
**/*.exe
# Local .terraform directories
.terraform/

# Delete or ignore all tfstate files
*.tfstate
*.tfstate.*

# Crash logs
crash.log

# k8s secret manifests
infra/k8s/*secret*.yaml

# Terraform variable files
*.tfvars

# generated, environment-specific configs (rebuilt via sed, go stale across cluster recreates)
infra/k8s/config

# script-managed image tag state, not real infra config
infra/terraform/.image_tag